/**
 * Version tags recorded on `Consent` rows (spec §10.1). There is no versioned legal
 * text served yet (spec §10.8, "Textos de Termos de Uso e Política de Privacidade
 * versionados em arquivo, servidos pela API") — that endpoint needs actual copy to
 * serve and is deliberately left for when that copy exists. Bump these constants
 * whenever the Terms or Privacy Policy text changes.
 */
export const CURRENT_TERMS_VERSION = 'v1';
export const CURRENT_PRIVACY_VERSION = 'v1';
